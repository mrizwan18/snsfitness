import { StyleSheet } from "react-native";
import { FacilityCard } from "../../AppStyles";

const styles = StyleSheet.create({
  container: FacilityCard.container,
  facilityContainer: FacilityCard.facilityContainer,
  logoContainer: FacilityCard.logoContainer,
  facility: FacilityCard.facility,
  optionsContainer: FacilityCard.optionsContainer,
  title: FacilityCard.title,
  logo: FacilityCard.logo,
  h1: FacilityCard.h1,
  h2: FacilityCard.h2,
  option: FacilityCard.option,
  optionIcon: FacilityCard.optionIcon,
  optionTitle: FacilityCard.optionTitle,
});

export default styles;
